# 🎮 Настройка Unity проекта Aether Renaissance

Подробная инструкция по запуску и тестированию игры в Unity.

## 📋 Требования

- **Unity 2021.3 LTS** или новее (рекомендуется 2022.3 LTS)
- **TextMeshPro** пакет (устанавливается автоматически)
- Git для клонирования репозитория

## 🚀 Быстрый старт

### Шаг 1: Создание нового Unity проекта

1. Откройте **Unity Hub**
2. Нажмите **New Project**
3. Выберите шаблон **3D Core** или **3D (URP)**
4. Название проекта: `AetherRenaissance`
5. Нажмите **Create Project**

### Шаг 2: Копирование скриптов

1. Клонируйте репозиторий:
```bash
git clone https://github.com/bakhteegames-ai/AetherRenaissance.git
```

2. Скопируйте папку `Assets/Scripts` из репозитория в папку `Assets` вашего Unity проекта

### Шаг 3: Установка TextMeshPro

1. В Unity откройте **Window → Package Manager**
2. Найдите **TextMeshPro**
3. Нажмите **Install**
4. При первом импорте TMP выберите **Import TMP Essentials**

## 🔧 Настройка сцены

### Создание основной сцены

#### 1. Создайте пустую сцену
- **File → New Scene**
- Сохраните как `MainScene`

#### 2. Добавьте GameManager
1. В Hierarchy: **Create Empty** → назовите `GameManager`
2. Добавьте компонент `GameManager.cs`
3. В Hierarchy: **Create Empty** → назовите `ResourceSystem`
4. Добавьте компонент `ResourceSystem.cs`
5. Настройте параметры:
   - Solaris Per Second: `5`
   - Quartz Per Second: `2`
   - Base Overcharge Cooldown: `60`

#### 3. Создайте Terrain (местность)
1. **GameObject → 3D Object → Terrain**
2. Установите Layer Mask для системы строительства:
   - **Edit → Project Settings → Tags and Layers**
   - Добавьте новый Layer: `Terrain`
   - Выберите Terrain объект → установите Layer: `Terrain`

#### 4. Создайте BuildingSystem
1. В Hierarchy: **Create Empty** → назовите `BuildingSystem`
2. Добавьте компонент `BuildingSystem.cs`
3. В Inspector настройте:
   - **Terrain Layer**: выберите `Terrain` layer
   - **Grid Size**: `1`

## 🎨 Создание UI

### Основной Canvas

1. **GameObject → UI → Canvas**
2. Canvas Scaler установите на **Scale With Screen Size**
3. Reference Resolution: `1920 x 1080`

### Панель ресурсов (верхняя панель)

1. ПКМ на Canvas → **UI → Panel** → назовите `ResourcePanel`
2. Настройте RectTransform:
   - Anchor: **Top Stretch**
   - Height: `80`
3. Добавьте текстовые элементы:

**Solaris Text:**
- ПКМ на ResourcePanel → **UI → Text - TextMeshPro** → `SolarisText`
- Text: "Solaris: 0"
- Font Size: `24`
- Alignment: Left
- Position X: `100`

**Quartz Text:**
- То же самое → `QuartzText`
- Text: "Quartz: 0"
- Position X: `300`

**Units Text:**
- То же самое → `UnitsText`  
- Text: "Units: 0"
- Position X: `500`

**Buildings Text:**
- То же самое → `BuildingsText`
- Text: "Buildings: 0"
- Position X: `700`

### Кнопка Overcharge

1. ПКМ на Canvas → **UI → Button - TextMeshPro** → `OverchargeButton`
2. Position: правый верхний угол
3. Добавьте дочерний объект для заполнения (Fill):
   - ПКМ на Button → **UI → Image** → `FillImage`
   - Image Type: **Filled**
   - Fill Method: **Radial 360**
4. Добавьте текст кулдауна:
   - Найдите дочерний Text → `CooldownText`
   - Text: "Ready!"

### Панель уведомлений

1. ПКМ на Canvas → **UI → Panel** → `NotificationPanel`
2. Anchor: **Middle Center**
3. Добавьте Text:
   - **UI → Text - TextMeshPro** → `NotificationText`
4. По умолчанию отключите: `NotificationPanel` → Inspector → снимите галочку

### Настройка UIManager

1. Выберите Canvas
2. Добавьте компонент `UIManager.cs`
3. В Inspector перетащите элементы UI:
   - **Solaris Text** → SolarisText
   - **Quartz Text** → QuartzText
   - **Units Text** → UnitsText
   - **Buildings Text** → BuildingsText
   - **Overcharge Button** → OverchargeButton
   - **Overcharge Cooldown Text** → CooldownText (текст внутри кнопки)
   - **Overcharge Fill Image** → FillImage
   - **Notification Panel** → NotificationPanel
   - **Notification Text** → NotificationText

## 🏗️ Создание префаба здания (тестовый)

1. **GameObject → 3D Object → Cube** → назовите `TestBuilding`
2. Scale: `(2, 1, 2)`
3. Добавьте компонент `GameObject.cs` (наш скрипт)
4. Настройте параметры:
   - Object Name: "Test Building"
   - Max Health: `100`
   - Solar Cost: `50`
   - Quartz Cost: `25`
5. Создайте папку `Assets/Prefabs`
6. Перетащите `TestBuilding` в папку Prefabs
7. Удалите `TestBuilding` из сцены

### Настройка BuildingSystem с префабом

1. Выберите `BuildingSystem` в Hierarchy
2. В Inspector:
   - **Building Preview Prefab** → перетащите префаб `TestBuilding`

## 🎮 Тестирование

### Проверка систем

1. Нажмите **Play** в Unity
2. Проверьте:
   - ✅ Ресурсы Solaris и Quartz растут автоматически
   - ✅ Счетчики Units и Buildings равны 0
   - ✅ Кнопка Overcharge показывает "Ready!"

### Тестирование Overcharge

1. Нажмите кнопку **Overcharge**
2. Должно произойти:
   - ✅ Ресурсы начинают расти в 2 раза быстрее
   - ✅ Кнопка становится неактивной
   - ✅ Отображается кулдаун (60 секунд)
   - ✅ Показывается уведомление
   - ✅ Через 10 секунд скорость генерации возвращается к норме

### Тестирование строительства (расширенное)

Для полного тестирования системы строительства:

1. Создайте скрипт `TestController.cs`:
```csharp
using UnityEngine;

public class TestController : MonoBehaviour
{
    private BuildingSystem buildingSystem;
    public GameObject buildingPrefab;
    
    void Start()
    {
        buildingSystem = FindObjectOfType<BuildingSystem>();
    }
    
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.B))
        {
            buildingSystem.StartBuildingPlacement(buildingPrefab);
        }
    }
}
```

2. Добавьте скрипт на GameManager
3. Назначьте Building Prefab
4. Нажмите **Play** → нажмите **B** → двигайте мышью → ЛКМ для постройки

## 📊 Консоль Debug

Во время игры открывайте **Console** (Window → General → Console) для просмотра логов:
- `[GameManager]` - регистрация объектов
- `[ResourceSystem]` - изменения ресурсов
- `[BuildingSystem]` - размещение зданий

## ⚠️ Частые проблемы

### Проблема: UI не обновляется
**Решение:** Убедитесь, что все ссылки в UIManager назначены правильно

### Проблема: Кнопка Overcharge не работает
**Решение:** 
1. Проверьте EventSystem в сцене (должен быть автоматически)
2. Убедитесь что Canvas → Render Mode = Screen Space - Overlay

### Проблема: TextMeshPro текст розовый
**Решение:** Импортируйте TMP Essentials через Window → TextMeshPro → Import TMP Essential Resources

### Проблема: Строительство не работает
**Решение:**
1. Убедитесь что Terrain имеет Layer "Terrain"
2. Проверьте что BuildingSystem.TerrainLayer установлен правильно
3. Добавьте Collider на Terrain

## 🎯 Что дальше?

 После базовой настройки вы можете:

1. **Создать больше зданий** - добавьте разные 3D модели
2. **Добавить юниты** - создайте префабы с компонентом GameObject
3. **Реализовать боевую систему** - используйте методы TakeDamage()
4. **Улучшить UI** - добавьте иконки, анимации
5. **Создать карту** - используйте Terrain Tools

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте Console на ошибки
2. Убедитесь что все скрипты скомпилированы
3. Откройте Issue на GitHub

---

**Готово!** Теперь у вас есть рабочий прототип RTS игры! 🎉
